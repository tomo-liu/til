- VercelデプロイでReadyなのにURLが404になる問題
  - ビルドは成功・コードも問題なしなのに `https://taiwan-split.vercel.app` が404
  - `curl -I` でVercel層のエラーか確認できる：`X-Vercel-Error: NOT_FOUND` はNext.jsの404ではなくVercelのルーティング層のエラー
  - 原因1：Vercelチームの「Deployment Protection」が有効で、チーム配下の全URLが外部からブロックされていた（`*-tomo-lius-projects.vercel.app` は 401）
  - 原因2：`.vercel.app` はグローバルな早い者勝ちの名前空間。チームプロジェクトでは `<project>-<team>.vercel.app` が実際のURLで `taiwan-split.vercel.app` は別アカウントが使用中だった
  - `vercel inspect <url> --logs` でビルドログを確認できる
  - `vercel alias ls` でエイリアスの一覧と実際の向き先を確認できる
- NetlifyへNext.jsをデプロイする手順
  - `@netlify/plugin-nextjs` を devDependencies に追加するだけでServer ActionsやSSR・動的ルートも動く
  - `netlify.toml` に `publish = ".next"` と `[[plugins]] package = "@netlify/plugin-nextjs"` を書く
  - CLIで `netlify sites:create --name <name> --account-slug <slug>` でサイト作成
  - `netlify env:set KEY VALUE` で環境変数を設定してから `netlify deploy --build --prod` でデプロイ
  - Vercelと違いチーム認証の制限がなく、デプロイ直後から外部公開される
