- Next.js + Supabase で割り勘Webアプリ（taiwan-split）をフルスクラッチで作った
  - Next.js 16 App Router では `params` が `Promise<{ id: string }>` になっており、`await props.params` で取得する
  - ページコンポーネントの型は `PageProps<'/group/[id]'>` というグローバルヘルパーで書ける
  - `generateMetadata` でページごとに動的タイトルを設定できる（同じく `params` は await 必須）
- Supabase RLS のハマりポイント
  - UPDATE ポリシーを忘れると `error: null` のまま 0 行しか更新されず、エラーが全く出ない
  - `.update().eq(...).select('id')` で更新後に行を取得し、`updated.length === 0` で検知するのが確実
  - INSERT / DELETE ポリシーはあっても UPDATE が別途必要なことを忘れがち
- Supabase の embedded join（リレーション取得）
  - `select('*, payer:paid_by(id,name,color), splits:expense_splits(member_id)')` でネストしたデータを1クエリで取れる
  - 型は `as unknown as ExpenseWithDetails[]` でキャストが必要
- Supabase TypeScript 型定義の要件
  - `GenericSchema` を満たすには `Relationships: []`、`Views: Record<string, never>`、`Functions: Record<string, never>` を全テーブルに含める必要がある
  - これが欠けると `name does not exist in type 'never[]'` のような型エラーが出る
- 精算計算（最小振込数アルゴリズム）
  - 各メンバーの収支（balance）を計算し、最大債務者と最大債権者を貪欲にマッチさせる
  - 割り勘の端数は最初の N 人に 1 円ずつ分配して合計を必ず一致させる
- i18n を中央集権的に管理する方法
  - `lib/i18n.ts` に `as const` で全翻訳をまとめ、`i18n[lang].section.key` でアクセス
  - `Lang` 型を i18n.ts で定義し、hooks からは re-export して後方互換を保つ
- モバイルファーストのUIで気をつけること
  - `input type="number"` は `text-base`（16px以上）にしないと iOS でズームされる
  - タッチターゲットは `min-h-[44px]` を基準にする
  - モーダルはスマホでは画面下から出るボトムシート、PCでは中央配置（`items-end sm:items-center`）
